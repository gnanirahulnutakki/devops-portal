# Legacy Backstage Plugins

This directory contains preserved Backstage-era plugin source.

It is not part of the primary runtime that runs the current portal.

Use the root Next.js runtime and current docs first:

- `src/`
- `prisma/schema.prisma`
- `docs/architecture/CURRENT_RUNTIME_ARCHITECTURE.md`
- `docs/legacy/SOURCE_TREE_STATUS.md`
