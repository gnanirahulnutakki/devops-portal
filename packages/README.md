# Legacy Backstage Packages

This directory is preserved for historical reference.

It is not the active application runtime for `devops-portal`.

Use the root Next.js runtime instead:

- `src/`
- `prisma/schema.prisma`
- root `Dockerfile`
- root `docker-compose.yml`
- `helm/devops-portal/`

For details, see:

- `docs/architecture/CURRENT_RUNTIME_ARCHITECTURE.md`
- `docs/legacy/SOURCE_TREE_STATUS.md`
